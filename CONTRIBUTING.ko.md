# 기여

*[English](CONTRIBUTING.md)*

`nmts-recovery`는 [NMTS](https://nmts.me) 없이 월러스 저장소에서 파일을 되찾는 프로그램입니다 —
「파일이 이 서비스가 계속 있느냐에 달려 있지 않다」는 약속의 나머지 절반입니다. 이 문서는 여기서
무엇을 환영하고 무엇을 받을 수 없는지를 적습니다.

**이야기를 나누는 곳: [디스코드](https://discord.gg/pcmRkVmVZk).** 영어와 한국어 둘 다 읽습니다.

## 직접 빌드해 보기

Rust 1.75 이상. 다른 도구도, 빌드 스크립트도, 코드 생성도 없습니다.

```
cargo build --release
cargo test
```

⚠ **가장 값어치 있는 시험은 이 프로그램이 존재하는 이유 그 자체입니다.** 복구 목록과 계정 코드를
가지고 공개 애그리게이터를 상대로 돌려 보신 뒤, 나온 바이트가 들어갔던 바이트와 같은지 확인해
주십시오. 다르다면 그것이 쓸 값어치가 있는 신고입니다.

## 환영하는 것

- **결함 신고.** 무엇을 하셨고, 무엇이 일어났고, 그것을 다시 볼 수 있는 방법. 서식이 있습니다.
- **질문.** 형식이든 코드든, 확인해 보려는 보장이든.
- **의견.** 지금 설계가 틀렸다고 말씀하시는 것도 포함합니다.
- **직접 확인하신 것.** 빌드해 보시고, 시험을 돌려 보시고, 형식 문서를 읽어 보신 뒤 코드와 문서가
  어긋나는 자리를 알려 주시는 것 — 누구든 보내 주실 수 있는 것 중에 가장 값어치가 큽니다.

**영어 또는 한국어로 써 주십시오.** 둘 다 읽습니다.

## 코드를 보내는 법

**코드를 환영합니다.** pull request를 여시고, 거기에 한 줄만 적어 주십시오 — **설명 칸**이나
댓글, 어느 쪽이든 됩니다.

> I have read the Contributor License Agreement and I agree to it.

⭐**설명 칸이 낫습니다.** 깃허브에는 pull request 자체를 지우는 길이 없어서 설명에 적으신 글은
그대로 남습니다. 댓글은 쓰신 분이 지울 수 있습니다.

동의 절차는 그것이 전부입니다 — 서명도, 실명도, 주소도, 서식도 없습니다. 동의서는
[CLA.md](CLA.md)이고, [CLA.ko.md](CLA.ko.md)에 조항마다 무슨 뜻인지 한국어로 적어 두었습니다.
needmoretruth의 모든 저장소가 같은 동의서를 쓰므로 **한 번 동의하시면 됩니다.**

짧게 말하면: 쓰신 코드의 저작권은 계속 그분 것이고, 저희는 프로그램 전체를 한 사람이 들고 있을 수
있을 만큼의 허락을 받습니다. Apache-2.0이 맞지 않는 사정이 있는 분께 다른 조건을 드리려면 전체를
들고 있는 쪽만 그 제안을 할 수 있기 때문입니다.

⚠ **보내신 코드가 실제로 도착하는 길이 보통과 다릅니다. 이 저장소는 내보낸 사본입니다.** 코드는
다른 저장소에서 쓰이고 파일 단위로 여기 복사되므로, **여기서** 만든 커밋은 다음 내보내기에
덮어써집니다. 그래서 보내신 변경은 내보내기의 원본이 되는 쪽으로 들어가고, 다음 내보내기 때 여기
나타납니다. 그러면 pull request는 그 변경을 담은 커밋을 가리키며 닫힙니다 — **거절돼서가 아니라
들어가서 닫히는 것입니다.** 이름은 [CONTRIBUTORS.md](CONTRIBUTORS.md)에 적힙니다.

### 터미널만으로 처음부터 끝까지

깃허브 토큰만 있으면 **여기서부터는 브라우저가 필요 없습니다** — 서식도, 로그인 화면도, 누를
것도 없습니다. [`gh`](https://cli.github.com)를 깔고 로그인해 두셨다면:

```
gh repo fork needmoretruth/nmts-recovery --clone
cd nmts-recovery
git switch -c 무엇을-고치는지
# 고치신 뒤에:
git commit -am "무엇을 왜 바꿨는지"
git push -u origin 무엇을-고치는지
gh pr create --repo needmoretruth/nmts-recovery --title "..." --body \
  "무엇이 달라지는지, 왜, 그리고 도는 것을 어떻게 볼 수 있는지.

I have read the Contributor License Agreement and I agree to it."
```

⚠**`gh repo fork`가 먼저여야 합니다.** `gh pr create`도 「대신 갈라 드릴까요」라고 묻긴 하는데
**물을 화면이 있을 때만** 그렇고, 없으면 그냥 멈춥니다. 먼저 갈라 두면 어느 쪽이든 됩니다.

**`gh`가 없으시면** — 그냥 `git`으로 갈라 복제하고 가지를 밀어 넣은 뒤 pull request는 브라우저에서
여시거나, `git format-patch`가 만든 것을 **nmts@nmts.me** 로 보내 주십시오. 어느 쪽이든 위 동의
한 줄은 pull request나 메일에 필요합니다.

**pull request가 번거로우시면 diff를 이슈에 붙여 주셔도 됩니다.** 똑같이 읽습니다. 다만 그 코드를
쓰기 전에 위 동의 한 줄은 필요합니다.

**이 도구는 NMTS가 사라진 뒤 파일로 돌아가는 마지막 길**입니다. 그래서 변경은 그 눈으로 읽습니다 —
망이 죽었을 때, 입력이 잘린 파일일 때, 손에 인쇄된 종이 한 장뿐일 때 어떻게 되는가. 질문도 대개 그
모양으로 갑니다.

## 받을 수 없는 것

- 보내실 권리가 없는 것, 또는 알려 주지 않은 라이선스가 붙어 있는 것.
- 도는지 아닌지 알 방법이 없는 변경. 새 동작에는 시험이 함께 옵니다.
- 이미 잘 도는 것을 묻지 않고 새로 쓴 것. 무엇을 바꾸고 싶은지 이슈로 먼저 말씀해 주시면
  한나절을 아끼십니다.

## 태도

분명하고 정확하게 써 주십시오. 코드에 대한 반대는 환영하고, 사람을 공격하는 글은 환영하지 않습니다.
남을 모욕하는 글은 지우고 그 계정을 막습니다. 그 이상의 절차는 없으며, 있는 척하는 것은 아무도 지킬
수 없는 약속이 됩니다.

## 라이선스

Apache-2.0 — [LICENSE](LICENSE)를 봐 주십시오. 만드시는 것에 이 조건이 걸림돌이 된다면
**nmts@nmts.me** 로 이유와 함께 알려 주십시오: 무엇을 만들고 계시고, 라이선스의 어느 부분이
문제인지.
